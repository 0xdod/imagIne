const Comment = require('../models').Comment;

const createComment = reqData => {};

const findByRelatedImage = id => {};

const deleteComment = id => {};

const updateComment = (id, data) => {};

module.exports = {
	createComment,
	findByUniqueID,
	deleteComment,
	updateComment,
};
