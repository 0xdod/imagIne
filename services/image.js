const Image = require('../models').Image;

const createImage = reqData => {};

// const findByUniqueID = id => {

// };

const deleteImage = id => {};

const updateImage = (id, data) => {};

module.exports = { createImage, findByUniqueID, deleteImage, updateImage };
