{{> header this }}
    <main class="container">
      <div class="row">
        <div class="col-sm-8">
          {{> @partial-block}}
        </div>
        <aside class="col-sm-4">
          {{> stats this }} {{> popular this }} {{> comments this }}
        </aside>
      </div>
    </main>
{{> footer this }}